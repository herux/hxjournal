import { ToolactionServiceEvent } from './toolaction-service-event';

describe('ToolactionServiceEvent', () => {
  it('should create an instance', () => {
    expect(new ToolactionServiceEvent()).toBeTruthy();
  });
});
